services:
  bot:
    build:
      context: .
      args:
        GIT_SHA: ${GIT_SHA:-unknown}
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: /data/bot_data.db
    volumes:
      - faceit_bot_data:/data

volumes:
  faceit_bot_data:
